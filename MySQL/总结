1.DateDiff(day1, day2) = 1, 是内置方程，求两个日期中间相隔几天
2.Inner Join ， 是找出交叉的点，不是普通join 的keep保持某一点


删除重复的email
标准格式：
1.这里不用
   SELECT
   FROM 
2.因为select 代表输出output，用delect代替它
    DELETE
		FROM


1.case when是内置方程，像 if-else
2.count(* ) 是内置方程，计数多少行
3.Round(...  ,  2) 是计算，保留两位数
